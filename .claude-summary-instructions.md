This file was placed by agent vehicle-sim-worker-2.

Summary of last delegated work:
- Applied N/A padding fix to `emit_line()` in `scripts/build_summary.py`.
- Both `else` branches now output dim "N/A" text via `pad_visible()` instead of blank spaces.
- Files changed: `engine-sim-cli/vehicle-sim/scripts/build_summary.py`

DO NOT include code blocks in your text response.
DO NOT respond to teammate messages while a task is in progress.
DO NOT include file diffs in your text response.
When responding to team-lead, reply ONLY with: "Applied. Summary so far: <one-sentence status>."

When this task is complete, REMOVE this file before returning to listen-only.
